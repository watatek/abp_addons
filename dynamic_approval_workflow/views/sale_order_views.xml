<?xml version="1.0" encoding="UTF-8" ?>
<!-- Copyright 2026 Abdalrahman Shahrour
     License LGPL-3.0 or later (https://www.linkedin.com/in/shahrour) -->
<odoo>

    <record id="view_order_form_dynamic_approval" model="ir.ui.view">
        <field name="name">sale.order.form.dynamic.approval</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form/header" position="inside">
                <button name="action_submit_for_approval"
                        type="object"
                        string="Submit for Approval"
                        class="btn-secondary"
                        invisible="state not in ('draft', 'sent') or approval_state not in ('draft', 'returned')"/>
            </xpath>

            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_approval_requests"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-check-square-o">
                    <field name="approval_request_count"
                           string="Approvals"
                           widget="statinfo"/>
                </button>
            </xpath>

            <xpath expr="//group[@name='order_details']" position="inside">
                <field name="approval_state"
                       readonly="1"
                       widget="badge"
                       decoration-info="approval_state == 'draft'"
                       decoration-warning="approval_state == 'pending_approval'"
                       decoration-success="approval_state == 'approved'"
                       decoration-danger="approval_state == 'rejected'"
                       decoration-muted="approval_state == 'returned'"/>
            </xpath>
        </field>
    </record>

    <record id="sale_order_tree_dynamic_approval" model="ir.ui.view">
        <field name="name">sale.order.list.dynamic.approval</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="before">
                <field name="approval_state"
                       widget="badge"
                       optional="show"
                       decoration-info="approval_state == 'draft'"
                       decoration-warning="approval_state == 'pending_approval'"
                       decoration-success="approval_state == 'approved'"
                       decoration-danger="approval_state == 'rejected'"
                       decoration-muted="approval_state == 'returned'"/>
            </xpath>
        </field>
    </record>

</odoo>
